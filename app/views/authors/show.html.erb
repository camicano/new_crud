<h1><%= @author.name %></h1>

<% @books.each do |book| %>
	<a href='/authors/<%= @author.id %>/books/<%= book.id %>'><p><%= book.title %></p></a>
<% end %>

<%= link_to "edit", edit_author_path(@author) %>

<%= form_for @book do |f| %>
<%= f.text_field :title %>
<%= f.submit %>
<% end %>

